[package]
name = "sigma-compiler-derive"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
sigma-compiler-core = { path = "../sigma-compiler-core" }
syn = "2.0"
